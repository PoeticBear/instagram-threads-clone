三个代表性场景的结论现在都精确了：

- post_like → ⚠️ 建了通知(列表有/Δ=1) 但没推 WS 帧
- post_quote → ❌ 没建通知 (Δ=0)
- reply_mention → ⚠️ 未建【reply_mention】通知：Δ=1 来自联动事件 post_reply（关键：服务端响应里 mentioned_users:"1000383" 说明 mention 字段被接受了，但没生成 reply_mention 通知）

---
完成：6 个场景的手动探针已就绪

用法（6 条命令，一条一个场景）

python3 wstest/send_notify.py post_like
python3 wstest/send_notify.py post_reply
python3 wstest/send_notify.py post_repost
python3 wstest/send_notify.py post_quote
python3 wstest/send_notify.py reply_like
python3 wstest/send_notify.py reply_mention

每条命令输出什么（可直接整段复制发后端）

① 场景说明 + 期望 event_type
② 登录 14dev(触发方) / 12dev(接收方)
③ 接收方建全新目标帖/回复（每次零配置，规避「已点赞」去重）
④ 触发方动作的【完整 HTTP 请求】method/URL/headers/body + 服务端响应
⑤ 接收方未读 Δ（服务端建没建通知）
⑥ 接收方通知列表是否真有对应类型条目（区分 Δ 是否被联动事件误导）
⑦ 接收方 WS 收到的所有帧 + 期望事件是否到达
⑧ 一句话结论

token 已脱敏（eyJ…（已脱敏）），日志里不会泄露活 token。

约定（请记一下，和 post_mention 时相反）

- 触发方 = 14dev，接收方 = 12dev（脚本自动登录两账号）。
- 你要同步看真机 app 的话，把 app 登录成 12dev，触发后看 flutter run 里有没有 [WS] event type=<场景>。脚本本身也会自动连 12dev 的 WS 复核，不依赖 app。

本次全部 6 场景实测结论（给后端的移交清单）

┌───────────────┬───────┬──────┬──────┬─────────────────────────────────────────────────────────────────────────────────────────────────────┐
│     场景      │ 未读Δ │ 列表 │ WS帧 │                                                结论                                                 │
├───────────────┼───────┼──────┼──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ post_like     │ +1    │ ✓    │ ✗    │ 建了通知，不推 WS                                                                                   │
├───────────────┼───────┼──────┼──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ post_reply    │ +1    │ ✓    │ ✗    │ 建了通知，不推 WS（只推了 reply_create 广播）                                                       │
├───────────────┼───────┼──────┼──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ post_repost   │ +1    │ ✓    │ ✗    │ 建了通知，不推 WS                                                                                   │
├───────────────┼───────┼──────┼──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ reply_like    │ +1    │ ✓    │ ✗    │ 建了通知，不推 WS                                                                                   │
├───────────────┼───────┼──────┼──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ post_quote    │ 0     │ ✗    │ ✗    │ 压根没建通知（quote_post_id 被接受、返回里也有，但没生成通知）                                      │
├───────────────┼───────┼──────┼──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ reply_mention │ +1*   │ ✗    │ ✗    │ 没建 reply_mention（mention 字段被接受 mentioned_users:"1000383"，但未生成通知；Δ 来自 post_reply） │
└───────────────┴───────┴──────┴──────┴─────────────────────────────────────────────────────────────────────────────────────────────────────┘

▎ 一句话给后端：5 个事件（post_like/post_reply/post_repost/reply_like）通知已建但漏推 WS；post_quote 根本没建通知；reply_mention 的 /post/reply 接受了 mention 字段却没建通知。请像 post_mention 那样补齐「建通知时同步推 WS 帧」。

---
脚本位置：wstest/send_notify.py（本地，含 dev 密码，不提交 git，和 send_as_12dev.py 一样）。

⚠️ 注意：每次运行会在 dev 服务器留下测试帖/回复（已累积一批 probe-* 帖子），dev 环境无妨。

要不要我把这次的实测结论追加进 docs/ws-notification-scenario-guide.md 的状态表，并更新 memory？这样下个对话 / 后端修复后复验时，直接有据可查。