# v1.0.0+22

> 发布日期：2026-06-22（追溯补档日期：2026-07-16）
> 前一构建：v1.0.0+21
> 状态：🟢 已发布

> 本版核心主题：**App Store 正式发布流水线首接入** —— `client/scripts/appstore-release.sh` 一键发布正式上架版本所需的 fastlane + API Key 配套、补齐 `.gitignore` 防误提交、以及 build 1.0.0+22 同步。

> 注：本 changelog 文件为追溯补档（项目当时没有对应 +22 版本的 changelog 文件，由 +23 发版流程一起回填）。

## ✨ 新增功能

- **App Store 正式发布流水线（fastlane）**（`4a681f1`）— 新增 `client/fastlane/` 整套配置 + `client/scripts/appstore-release.sh` 一键脚本：自动调用 `fastlane ios upload_listing` + `submit_for_review`，把 IPA 推到 App Store Connect 正式分发位。完整快速发布命令、首次 API Key 配置前置条件、安全红线均写入 `CLAUDE.md`「一键发布到 App Store 正式环境」一节。

## 🐛 修复

- （无）

## ⚡ 性能优化

- （无）

## ♻️ 重构

- （无）

## 🎨 样式调整

- （无）

## 📝 文档

- **`CLAUDE.md` 增「一键发布到 App Store」工作流**（`4a681f1`）— 收录 `client/scripts/appstore-release.sh` 触发指令、首次配置步骤、参数化（`--no-bump` / `--only-upload` / `--no-submit`）、与 `release.sh`（TestFlight）的对照表、失败处理。

## 🔧 构建 / CI

- **新增 `client/fastlane/` 正式发布配置**（`4a681f1`）— `Fastfile` / `Appfile` / `metadata/{en-US,zh-Hans}/*.txt` / `screenshots/` 布局；`api_key.json` / `auth/*.p8` 由 `.gitignore` 排除。
- **`.gitignore` 排除 fastlane 生成的 `report.xml`**（`0f9a777`）— 避免 fastlane 跑 lint 之类后留下临时报告被误提交。
- **构建号 1.0.0+21 → 1.0.0+22**（`29c43e6`）— 准备 App Store 正式发布。

---

**相关 commit**

- `4a681f1` — feat: 接入 App Store 正式发布流水线 — Fastlane + 一键脚本
- `0f9a777` — chore: 忽略 fastlane 生成的 report.xml
- `29c43e6` — chore: bump build to 1.0.0+22 for App Store release
