.DS_Store
error-info.md
pubspec-origin.lock
client/ios/Runner.xcworkspace/xcshareddata/swiftpm/
client/ios/build/

# === Sensitive / secrets — 永不提交 ===
# Claude Code 本地密钥配置（含 ANTHROPIC_AUTH_TOKEN 等 token）
.claude/settings.json
.claude/settings.local.json

# 环境变量 / 凭证文件
.env
**/.env
credentials.json
secrets.json

# iOS / 签名证书与描述文件
*.p12
*.mobileprovision
*.cer
*.pem
*.key

# Android 签名密钥
*.keystore
*.jks
key.properties

# iOS 崩溃日志（可能含设备 / 用户信息）
*.ips

# Google OAuth 凭据文件（从 Google Cloud 下载、含 client_id；按官方不放工程）
client/ios/Runner/client_*.plist

# WS 联调脚本（含本地测试账号，仅本地使用，不入库）
wstest/
